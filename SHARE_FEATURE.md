# Функция "Поделиться" - Создание челленджей

## Описание

Функция позволяет создавать ссылки с предустановленными настройками тренажёра и фиксированным количеством задач. Человек, который переходит по ссылке, должен решить указанное количество задач без ошибок.

## Как использовать

### Создание челленджа

1. Откройте любой тренажёр (например, "Таблица умножения")
2. Настройте параметры тренажёра (выберите нужные таблицы, установите диапазоны и т.д.)
3. Нажмите на кнопку 🔗 в правом верхнем углу
4. В модальном окне выберите количество задач (от 5 до 50)
5. Нажмите "Скопировать ссылку"
6. Поделитесь ссылкой с другом

### Прохождение челленджа

1. Перейдите по полученной ссылке
2. Тренажёр автоматически откроется с указанными настройками
3. Решайте задачи без ошибок
4. При первой ошибке челлендж завершится неудачей
5. После успешного решения всех задач появится сообщение о победе

## Технические детали

### Архитектура решения

Функциональность реализована модульно и легко расширяется на другие тренажёры:

1. **ShareLinkUtil** (`src/utils/shareLink.js`) - утилита для кодирования/декодирования настроек в URL
2. **Модальное окно** - UI компонент в `BaseTrainerComponent`
3. **Режим челленджа** - логика в `BaseTrainer`
4. **Обработка URL** - в `app.js` при инициализации

### Формат URL

```
https://example.com/?trainer=multiplication-table&tasks=10&settings=<base64>
```

Параметры:
- `trainer` - имя тренажёра
- `tasks` - количество задач
- `settings` - настройки тренажёра в формате base64(JSON)

### Добавление кнопки "Поделиться" в новый тренажёр

1. В компоненте тренажёра добавьте кнопку:
```javascript
<button id="${trainerName}-share-btn" class="icon-button">🔗</button>
```

2. В `render()` добавьте модальное окно:
```javascript
render() {
    this.innerHTML = `
        ${this.getMainScreenTemplate()}
        ${this.getSettingsTemplate()}
        ${this.getShareModalTemplate()}  // <- Добавить
    `;
}
```

3. В `initDOM()` добавьте элемент кнопки:
```javascript
this.elements = {
    // ... другие элементы
    shareBtn: document.getElementById('${trainerName}-share-btn'),
};
```

4. Вызовите инициализацию модального окна:
```javascript
this.initShareModalHandlers();
```

Всё! Базовый класс `BaseTrainer` уже содержит всю необходимую логику.

## Ограничения

- Минимум задач: 5
- Максимум задач: 50
- При ошибке челлендж сразу завершается
- Нельзя открыть настройки в режиме челленджа

## Будущие улучшения

- Добавить таймер для прохождения челленджа
- Сохранять результаты челленджей
- Создавать рейтинговые таблицы
- Поделиться результатом в социальных сетях
