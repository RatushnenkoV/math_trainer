# Настройка для Telegram Mini App

## Важно! Замените имя бота

В файле [src/utils/shareLink.js](src/utils/shareLink.js) найдите строку:

```javascript
const botUsername = 'rat_math_trainer_bot'; // Замените на ваше имя бота
```

И замените `rat_math_trainer_bot` на **ваше** имя бота.

## Как работает шаринг в Telegram

1. **Пользователь создаёт челлендж:**
   - Открывает настройки тренажёра
   - Нажимает "🔗 Создать ссылку"
   - Выбирает количество задач
   - Нажимает "Поделиться"

2. **Что происходит:**
   - Если доступен метод `switchInlineQuery` - откроется меню выбора чата
   - Если нет - ссылка копируется в буфер обмена + показывается popup

3. **Формат ссылки:**
   ```
   https://t.me/ваш_бот?startapp=trainer=multiplication-table&tasks=10&settings=...
   ```

4. **Друг открывает ссылку:**
   - Mini App запускается автоматически
   - Параметры читаются из `window.Telegram.WebApp.initDataUnsafe.start_param`
   - Тренажёр загружается с нужными настройками в режиме челленджа

## Тестирование

1. Откройте Mini App в Telegram
2. Зайдите в любой тренажёр (например, таблица умножения)
3. Откройте настройки (⚙)
4. Нажмите "🔗 Создать ссылку"
5. Выберите количество задач
6. Нажмите "Поделиться"
7. Отправьте себе в "Избранное"
8. Откройте ссылку - должен запуститься челлендж

## Отладка

Если что-то не работает, откройте консоль браузера (в Telegram Web или через DevTools):

```javascript
// Проверьте, что параметры передаются:
console.log(window.Telegram.WebApp.initDataUnsafe);

// Должно быть примерно так:
{
  start_param: "trainer=multiplication-table&tasks=10&settings=...",
  ...
}
```

## Что нужно настроить на стороне бота

В вашем Telegram боте (`@rat_math_trainer_bot`) убедитесь, что:

1. **Mini App настроен:**
   - В BotFather выполните `/setmenubutton`
   - Укажите URL вашего Mini App
   - Тип кнопки: `web_app`

2. **Inline режим включён (опционально):**
   - Для работы `switchInlineQuery`
   - В BotFather: `/setinline`
   - Настройте placeholder

## Ограничения

- `switchInlineQuery` доступен только в Telegram версии 6.0+
- Для старых версий работает fallback через буфер обмена
- Start параметр имеет ограничение по длине (~256 символов), поэтому мы используем base64

## Возможные проблемы

1. **"Ссылка не работает"**
   - Проверьте имя бота в `shareLink.js`
   - Убедитесь, что Mini App настроен в BotFather

2. **"Челлендж не запускается"**
   - Откройте консоль и проверьте `initDataUnsafe.start_param`
   - Проверьте, что параметры корректно декодируются

3. **"Кнопка не появляется"**
   - Проверьте, что вы в экране настроек
   - Убедитесь, что стили загружены
